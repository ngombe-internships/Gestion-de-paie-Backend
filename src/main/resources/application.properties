spring.application.name=Paie1
server.port=8081

# Base de donnees MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/hades?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT
app.jwt-secret=HadesSecretKeyWithAtLeast32CharactersLong123!
app.jwt-expiration-milliseconds=86400000

# Logging
logging.level.com.hades.paie1=INFO

# Upload
file.upload-dir=C:/uploads/logos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cloudinary
cloudinary.cloud_name=dfurjzy3b
cloudinary.api_key=822931638535691
cloudinary.api_secret=6zyFl5vn9iCoWNrrt4OMl_QEci0

# Jackson - CONFIGURATION ESSENTIELLE POUR SWAGGER
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false

# Encoding - CRUCIAL
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true
server.servlet.encoding.enabled=true

# OpenAPI/Swagger - CONFIGURATION SIMPLIFIÉE
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# DÉSACTIVER LA COMPRESSION POUR DEBUG
server.compression.enabled=false

# Content Type - FORCER JSON
spring.mvc.contentnegotiation.favor-parameter=false
spring.mvc.contentnegotiation.favor-path-extension=false

# Erreurs
server.error.include-message=always